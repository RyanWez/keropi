# Runtime dependencies. Kept in sync with [project.dependencies] in pyproject.toml
# because Render's Python runtime installs from this file.
#
# Dev/test extras: pip install -e ".[dev]"
aiogram>=3.31,<4
aiohttp>=3.14,<4
qrcode>=8.2,<9
pillow>=12.0,<13
python-dotenv>=1.2,<2
